[project]
name = "nba-cockroach-db"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "polars>=1.44.1",
    "sports-hub",
]

[tool.uv.sources]
sports-hub = { git = "https://github.com/shaggycamel/sports-hub.git" }
