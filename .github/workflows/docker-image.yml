name: Update Cockroach DB

on:
  schedule:
    - cron:  '0 3 * * *'

jobs:
    compile:
        name: Compile nba cockroach db
        runs-on: ubuntu-latest
        steps:
          - name: Run the build process with Docker
            uses: docker://shaggycamel/nba_cockroach_db